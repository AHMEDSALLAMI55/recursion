PROCEDURE isPalindrome(left,right:INTEGER,word:STRING[50])

BEGIN
IF(word.length =0 or left = right ) then
write("it's palindrome")
ELSE 
IF (word[left]= word[right]) then
is palindrome(left+1, right-1,word)
ELSE
write ("it's not palindrome")
END-IF
